<%= provide(:title, @donor.name) %>
<div class="row">
  <aside class="col-md-4">
    <section class="donor_info">
      <h1>
        <%= gravatar_for @donor %>
        <%= @donor.name %>
      </h1>
    </section>
  </aside>
</div><br><br>